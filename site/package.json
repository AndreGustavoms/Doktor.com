{
  "name": "doktor-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --outfile=assets/app.js",
    "dev": "node server.mjs",
    "start": "node server.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
