export default function DashboardPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 p-8">
      <h1 className="font-[family-name:var(--font-display)] text-4xl font-bold text-chalk">
        Painel GitHub
      </h1>
      <p className="font-[family-name:var(--font-mono)] text-sm uppercase tracking-[0.08em] text-chalk-dim">
        Fase 0 — fundação de segurança
      </p>
    </main>
  );
}
