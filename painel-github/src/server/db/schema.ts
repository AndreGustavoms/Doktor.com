import "server-only";

/*
 * Esquema do banco local (SQLite + Drizzle) — preenchido na Fase 1.
 * Ver docs/ARCHITECTURE.md e prompt-painel-github-local.md §6 para o
 * desenho completo: auth, sessions, apiCache, pinned, tags, notes,
 * activityLog, portfolioConfig, portfolioItems, readState.
 */
